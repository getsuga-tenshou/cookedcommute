# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Env / secrets
.env
*.local
rsa_key.p8
rsa_key.pub
*.p8

# Data lake (raw landing) & local artifacts
lake/
*.gz
sample_data/*.live.json

# dbt (profiles.yml is committed but reads only env vars — no secrets)
dbt/target/
dbt/dbt_packages/
dbt/logs/

# Terraform
infra/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log

# OS / editor
.DS_Store
.idea/
.vscode/

# --- Not pushed to the public repo (setup/creation scripts, sample data, cloud-only, personal) ---
# Snowflake setup DDL (01/02/03 — create DB, warehouse, tables, views, stages)
snowflake/
# Key-pair setup helper (generates the RSA key)
scripts/gen_snowflake_key.py
# Sample / captured data
sample_data/
# Terraform (cloud path only)
infra/
# Azure Functions (cloud path only)
azure_functions/
# Runbook + architecture notes (personal)
docs/
# Planning / business-case doc (personal)
PROJECT_PLAN.md
# Deprecated Postgres-era leftovers (superseded by Snowflake)
db/
docker-compose.yml
