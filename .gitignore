# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Secrets — NEVER commit
.env
*.local
*.p8
rsa_key.pub

# Data lake (raw landing) & local artifacts
lake/
*.gz
sample_data/

# dbt build artifacts
dbt/target/
dbt/dbt_packages/
dbt/logs/

# Terraform — commit the IaC (*.tf), ignore state & secrets
infra/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log

# Azure Functions — commit the function defs, ignore local settings & the copied package
azure_functions/local.settings.json
azure_functions/ingestion/
azure_functions/.python_packages/

# Personal / deprecated (not part of the product)
PROJECT_PLAN.md
docs/
db/
docker-compose.yml
RESUME_NOTES.md

# OS / editor
.DS_Store
.idea/
.vscode/
